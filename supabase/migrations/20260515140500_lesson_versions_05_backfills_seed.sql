-- =============================================================================
-- LESSON VERSIONS — 05_backfills_seed
-- Intentionally no DB backfill: baseline lesson_versions come from publish/RPC
-- or follow-on data migrations (docs/architecture/db_principles.md: backfills
-- are explicit and optional when product chooses app-driven seeding).
-- =============================================================================

SELECT 1;
