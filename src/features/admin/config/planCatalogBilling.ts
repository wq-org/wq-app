/** Values for `plan_catalog.billing_interval` (text; DB is NOT NULL — use `none` where product means “no recurring cadence”). */
export const PLAN_BILLING_NONE = 'none'
export const PLAN_BILLING_MONTHLY = 'monthly'
export const PLAN_BILLING_YEARLY = 'annual'
