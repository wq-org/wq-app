/** Scroll target for the active gameplay segment inside If/Else continuous preview. */
export const IF_ELSE_GAMEPLAY_ANCHOR_ATTR = 'data-if-else-gameplay-anchor'

export const IF_ELSE_GAMEPLAY_ANCHOR_SELECTOR = `[${IF_ELSE_GAMEPLAY_ANCHOR_ATTR}]`

/** Wrapper for a newly mounted segment (divider, branch node, etc.) in the session shell. */
export const IF_ELSE_SEGMENT_ANCHOR_ATTR = 'data-if-else-segment-anchor'

export const IF_ELSE_SEGMENT_ANCHOR_SELECTOR = `[${IF_ELSE_SEGMENT_ANCHOR_ATTR}]`
